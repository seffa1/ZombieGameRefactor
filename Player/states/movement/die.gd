extends "res://Libraries/state.gd"

"""
Although it looks like this state isn't written yet, this is actually the state.
It is just here to prevent movement controlls. The death animation / effect
takes place in the action's death state.
"""

# Initialize the state. E.g. change the animation
func enter():
	return

# Clean up the state. Reinitialize values like a timer
func exit():
	return

func handle_input(_event):
	return

func update(_delta):
	return
